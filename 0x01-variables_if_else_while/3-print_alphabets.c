#include <stdio.h>

/**
 * main - Entry point
 * A program that prints the alphabet
 *Return: Always 0 (Success)
 */

int main(void)
{
	char c;
	char d;

	c = 'a';
	d = 'A';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
